---
export interface Props {
  id: string;
  title: string;
  img: string;
  lanzamiento: string;
  descripcion: string;
  genero: string;
}

const { id, title, lanzamiento, descripcion, genero, img } = Astro.props;
---

<div class="card">
  <a href={`/${id}`}>
    <div class="card-wrapper">
      <img src={img} alt="" />
      <div>
        <h3>{title}</h3>
        <p>{genero}</p>
        <p>{lanzamiento}</p>
      </div>
    </div>
    <p class="text">{descripcion.slice(0,90)}...</p>
  </a>
</div>

<style>
  .card {
    background: #fff;
    box-shadow: 0px 4px 4px 2px rgba(0, 0, 0, 0.25);
    margin-bottom: 1rem;
    border-radius: 8px;
    padding: 0.7rem;
  }

  .card-wrapper {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
  }
  .card-wrapper > img {
    max-width: 150px;
    object-fit: cover;
    border-radius: 7px;

  }
</style>
